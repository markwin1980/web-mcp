# ========================================
# MCP 服务器配置
# ========================================

# MCP 服务器端口（SSE 模式）
# 默认值：8000
MCP_SERVER_PORT=8000

# ========================================
# 浏览器服务配置
# ========================================

# 是否使用无头模式（不显示浏览器窗口）
# 默认值：false
# 可选值：1, true, yes, on（启用无头模式）| 其他任何值（关闭无头模式，显示浏览器窗口用于调试）
BROWSER_HEADLESS=false

# 最大缓存页面数量
# 默认值：10
# 最大值：100（超过会被自动限制为 100）
# 说明：页面池中最多保留的空闲页面数量，超出时会清理最旧的未使用页面
BROWSER_MAX_CACHED_PAGES=10

# 初始页面数量
# 默认值：1
# 最大值：10（超过会被自动限制为 10）
# 说明：浏览器启动时预先创建的页面数量
BROWSER_INITIAL_PAGE_COUNT=1

# 浏览器视口宽度
# 默认值：1280
# 说明：浏览器窗口的宽度（像素）
BROWSER_VIEWPORT_WIDTH=1280

# 浏览器视口高度
# 默认值：720
# 说明：浏览器窗口的高度（像素）
BROWSER_VIEWPORT_HEIGHT=720
